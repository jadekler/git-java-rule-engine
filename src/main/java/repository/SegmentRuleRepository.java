package repository;

import model.SegmentRule;

import java.util.List;

import static java.util.Collections.emptyList;

public class SegmentRuleRepository {
    // Up to your implementation (get from db, file, input, whatever)
    public List<SegmentRule> getAll() {
        return emptyList();
    }
}
