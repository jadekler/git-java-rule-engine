package repository;

import model.Product;

import java.util.List;

import static java.util.Collections.emptyList;

public class ProductRepository {
    // Up to your implementation (get from db, file, input, whatever)
    public List<Product> getAll() {
        return emptyList();
    }
}
